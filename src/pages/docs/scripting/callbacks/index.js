import React from "react";
import { Redirect } from "@docusaurus/router";

export default () => {
  // redirect to first callback for now, should be changed later
  // TODO: create an article and explain what callbacks/events are and how to use or create them
  return <Redirect to="/docs/scripting/callbacks/OnActorStreamIn" />;
};