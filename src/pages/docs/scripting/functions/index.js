import React from "react";
import { Redirect } from "@docusaurus/router";

export default () => {
  // redirect to first function for now, should be changed later
  // TODO: create an article and explain what functions are and how to use or create them
  return <Redirect to="/docs/scripting/functions/AddCharModel" />;
};